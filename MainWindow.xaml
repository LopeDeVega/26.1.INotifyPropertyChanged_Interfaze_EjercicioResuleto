<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <StackPanel Margin=" 30" Background="Beige">


            <Label Foreground="DarkCyan" FontSize="15">Name</Label>
            <!--Data Binding is related to the text box (Nombre sera creado en una clase)-->
            <TextBox BorderBrush="Gold" Width="250" Height="50" Name="txtName" Text="{Binding Path=Name, Mode=TwoWay}"></TextBox>

            <Label Foreground="DarkCyan" FontSize="15">Surname</Label>
            <TextBox BorderBrush="Gold" Width="250" Height="50" Text="{Binding Path=Surname, Mode=TwoWay}"></TextBox>

            <Label Foreground="DarkCyan" FontSize="15">Full Name</Label>
            <TextBox BorderBrush="Gold" Width="250" Height="50" Text="{Binding Path=FullName, Mode=TwoWay}"></TextBox>




        </StackPanel>




    </Grid>
</Window>
